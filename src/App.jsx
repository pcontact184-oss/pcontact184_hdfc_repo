// Your updated src/App.jsx content goes here

// Example:
const reportSend = () => {
    const accountIds = selectedAccountsEffective;
    const emailsList = Array.isArray(emails) ? emails : emailsCsv.split(',');
    // rest of the function logic
};

// Ensure the existing UI code remains intact
